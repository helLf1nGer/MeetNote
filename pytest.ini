[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    network: contacts a real network service (needs --run-network)
filterwarnings =
    ignore::DeprecationWarning
